import 'package:flutter/material.dart';

class LandscapeBuilder extends StatefulWidget {
  const LandscapeBuilder({super.key});

  @override
  State<LandscapeBuilder> createState() => _LandscapeBuilderState();
}

class _LandscapeBuilderState extends State<LandscapeBuilder> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
